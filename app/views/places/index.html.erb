<% @places.each do |place| %>
  <div class="booyah-box col-xs-10 col-xs-offset-1">
    <h1><%= link_to place.name, place_path(place) %></h1><br />
    <i><%= place.address %></i><br />
    <p><%= place.description %></p><br />

    <% if place.user.present? %>
      <small><%= place.user.email %></small><br.>
    <% end %>

    <% if place.photos.present? %>
      <div class="col-xs-12 col-sm-6 col-md-2 food-image">
        <%= image_tag place.photos.last.picture, :class => "img-responsive center-block" %>
        
      </div>
    <% end %>

    <% if place.comments.present? %>
      <div class="col-xs-12">
        <blockquote>
          <% if place.comments.last.humanized_rating.present? %>
            <b><%= place.comments.last.humanized_rating %></b> -
          <% end %>
          <%= place.comments.last.message %>
          <small><% place.comments.last.user.email %></small>
        </blockquote>
      </div>
    <% end %>
  </div>
<% end %>
<div class="col-xs-10 col-xs-offset-1 paginator">
  <%= will_paginate @places, renderer: BootstrapPagination::Rails %>
</div>
